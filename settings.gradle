include(
	':app',
	':shared-error',
	':shared-user'
)
rootProject.name = 'HomeBookkeeping'

project(':shared-error').projectDir = file('shared-error')
project(':shared-user').projectDir = file('shared-user')